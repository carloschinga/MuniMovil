<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.gob.munihuacho.munimovil">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/muni">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/muni.NoActionBar" />
        <activity
            android:name=".activities.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/muni.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/muni.NoActionBar" />
        <activity android:name=".activities.OperacionVehiculoActivity" />
        <activity android:name=".activities.CcCtaCteActivity" />
        <activity android:name=".activities.CcCtaAhorroActivity" />
        <activity android:name=".activities.RegistroCivilActivity" />
        <activity android:name=".activities.SisgedoActivity" />
        <activity android:name=".activities.PapeletasActivity" />
        <activity android:name=".activities.PapeletaDetailActivity" />
        <activity android:name=".activities.NacimientoActivity" />
        <activity android:name=".activities.NacimientoDetailActivity" />
        <activity android:name=".activities.MatrimonioActivity" />
        <activity android:name=".activities.MatrimonioDetailActivity" />
        <activity android:name=".activities.DefuncionActivity" />
        <activity android:name=".activities.DefuncionDetailActivity" />
        <activity android:name=".activities.CuentaCorrienteActivity" />
        <activity android:name=".activities.CtaCteDetailActivity"></activity>
    </application>

</manifest>